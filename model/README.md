# Add your Teachable Machine model here

Copy the complete TensorFlow.js export here: `model.json`, `metadata.json`, and every binary shard referenced by `model.json`.

The export must contain six labels in this exact order: `Scenario 1`, `Scenario 2`, `Scenario 3`, `Scenario 4`, `Scenario 5`, `Scenario 6`.
