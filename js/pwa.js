export async function register(){if(!("serviceWorker"in navigator))return;try{await navigator.serviceWorker.register("./sw.js",{scope:"./"})}catch(error){console.warn("Service worker registration failed",error)}}export function installButton(){const button=document.getElementById("installButton");let prompt;addEventListener("beforeinstallprompt",event=>{event.preventDefault();prompt=event;button.classList.remove("hidden")});button.addEventListener("click",async()=>{if(!prompt)return;prompt.prompt();await prompt.userChoice;prompt=null;button.classList.add("hidden")});addEventListener("appinstalled",()=>button.classList.add("hidden"))}
