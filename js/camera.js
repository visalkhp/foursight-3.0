import{state}from"./state.js";import{cameraControls,status}from"./ui.js";const video=document.getElementById("camera");export async function startCamera(){if(!navigator.mediaDevices?.getUserMedia)throw new Error("Camera access is not supported by this browser.");status("camera","Requesting access","busy");try{const stream=await navigator.mediaDevices.getUserMedia({video:{facingMode:"environment"},audio:false});video.srcObject=stream;await video.play();state.setStream(stream);cameraControls(true);status("camera","Running","good")}catch(error){status("camera","Unavailable","error");throw new Error(error.name==="NotAllowedError"?"Camera permission was denied.":`Unable to start the camera: ${error.message}`)}}export function stopCamera(){state.stream?.getTracks().forEach(track=>track.stop());video.srcObject=null;state.setStream(null);cameraControls(false);status("camera","Stopped")}export function videoElement(){return video}
